{
  "mcpServers": {
    "codex-mobile-telegram": {
      "command": "bash",
      "args": [
        "-lc",
        "workspace=\"${CODEX_MCP_WORKSPACE:-$PWD}\"; exec docker run --rm -i --pull=missing --cap-drop=ALL --security-opt=no-new-privileges --read-only --tmpfs /tmp:rw,noexec,nosuid,size=256m --pids-limit=256 -v \"$workspace:/workspace\" -v codex-mobile-telegram:/state -e CODEX_MCP_HOST_WORKSPACE=\"$workspace\" -e TELEGRAM_API_ID -e TELEGRAM_API_HASH ghcr.io/ciurlaro/codex-mobile-plugins:1.0.0 telegram"
      ]
    }
  }
}
